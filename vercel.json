{
  "buildCommand": "ng build --configuration=production",
  "outputDirectory": "dist/front-steam-stats/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}